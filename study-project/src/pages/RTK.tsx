import React from 'react';
interface rtkProps {

}

const rtk: React.FC<rtkProps> = ({}) => {

    return (
        <div className="rtk">
            <h1>React hook form</h1>
        </div>
    );
};

export default rtk;